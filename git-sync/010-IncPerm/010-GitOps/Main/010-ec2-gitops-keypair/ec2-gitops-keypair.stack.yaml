template-file-path: git-sync/010-IncPerm/010-GitOps/Main/010-ec2-gitops-keypair/ec2-gitops-keypair.cform.yaml

parameters: 
    TenantId: GitOps
    EnvId: Main

tags:
  TenantId: GitOps
  EnvId: Main

# sync-test: trigger one-file update for CFN stack sync validation
