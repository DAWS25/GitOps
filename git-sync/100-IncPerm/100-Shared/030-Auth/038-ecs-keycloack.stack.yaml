template-file-path: git-sync/100-IncPerm/100-Shared/030-Auth/038-ecs-keycloack.cform.yaml

parameters:
    TenantId: Auth
    VPCTenantId: IncPerm
    DBMasterUserSecretId: ${KEYCLOAK_DB_MASTER_SECRET_ID}
    KeycloakAdminSecretId: ${KEYCLOAK_ADMIN_SECRET_ID}

tags:
    TenantId: Auth
