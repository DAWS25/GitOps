template-file-path: git-sync/100-IncPerm/100-GitOps/010-Main/010-source-credentials/010-source-credentials.cform.yaml

parameters:
  TenantId: GitOps
  EnvId: Main
  GitHubToken: ${GITHUB_TOKEN}

tags:
  TenantId: GitOps
  EnvId: Main
